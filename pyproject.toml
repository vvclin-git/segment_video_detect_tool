[project]
name = "hsv-video-segmentation-tester"
version = "0.1.0"
description = "Interactive HSV video segmentation tester with ROI and blob bounding boxes"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "opencv-python>=4.8",
    "numpy>=1.24",
    "Pillow>=10.0",
    "reportlab>=4.0",
]

[tool.uv]
package = false
